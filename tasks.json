{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and Run",
            "type": "shell",
            "command": "gcc",
            "args": ["compiler.c", "-o", "compiler.exe", && "./compiler.exe"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}